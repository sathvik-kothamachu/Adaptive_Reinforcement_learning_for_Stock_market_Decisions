# Backend API Server Requirements
# Install with: pip install -r requirements-backend.txt

# Core API Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Data Processing (should already be installed)
pandas>=1.5.0
numpy>=1.23.0

# ML/RL (should already be installed)
scikit-learn>=1.2.0
stable-baselines3>=2.0.0
gymnasium>=0.28.0

# Data Fetching (should already be installed)
yfinance>=0.2.32

# Technical Analysis (should already be installed)
ta>=0.10.2

# Sentiment Analysis (Upgraded to FinBERT)
transformers>=4.35.0
torch>=2.1.0
vaderSentiment>=3.3.2

# HMM for regime detection (should already be installed)
hmmlearn>=0.3.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0

# Local LLM Dependencies (Ollama)
ollama>=0.6.0
transformers>=4.41.0
torch>=2.2.0
sentencepiece
protobuf
